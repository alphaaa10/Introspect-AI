# ──────────────────────────────────────────────────────────────
# Sentinel backend — pinned dependency list
# Python 3.12 required (pydantic-core has no cp314 wheel yet).
# Install into the project virtualenv:
#     py -3.12 -m venv venv
#     venv\Scripts\pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# ── Foundation (this phase) ────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ── Testing ────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2           # ASGI test client for FastAPI route tests

# ── Phase 2 (ingestion / feature extraction) ──────────────────
pandas==2.2.3
# pyarrow==17.0.0

# ── Phase 3 (graph / ML) ──────────────────────────────────────
# torch==2.4.1
# torch-geometric==2.6.1
numpy==2.1.1
scikit-learn==1.5.2

# ── Phase 4 (explainability) ──────────────────────────────────
# shap==0.46.0

# ── Phase 7 utilities ─────────────────────────────────────────
# python-multipart==0.0.12   # file-upload endpoints

torch==2.4.1
